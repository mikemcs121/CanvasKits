const fs=require('fs'),path=require('path'),crypto=require('crypto');
const deps='../../../Fox Fall/tmp/vector-tools/node_modules/',Jimp=require(deps+'jimp'),potrace=require(deps+'potrace'),{Resvg}=require(deps+'@resvg/resvg-js');
const kit=path.resolve(__dirname,'../..'),asset=path.join(kit,'assets/8x10'),w=1200,h=1500,n=w*h;
(async()=>{
 const source=path.join(kit,'test/gnome-halloween-outline-8x10.png'),raw=await new Promise((resolve,reject)=>potrace.trace(source,{threshold:160,turdSize:2,alphaMax:1,optCurve:true,optTolerance:.08,color:'#000000',background:'transparent'},(e,s)=>e?reject(e):resolve(s)));
 const tag=raw.match(/<path\b[^>]+\/>/)[0],svg=`<svg xmlns="http://www.w3.org/2000/svg" width="8in" height="10in" viewBox="0 0 2400 3000"><title>Halloween Gnome - 8 x 10 transfer outline</title>${tag}</svg>`;
 if(/<image|<rect|data:image/.test(svg))throw Error('Non-vector content');fs.writeFileSync(path.join(__dirname,'gnome-halloween-outline-8x10.svg'),svg);
 const native=new Resvg(svg,{background:'white',fitTo:{mode:'width',value:w},font:{loadSystemFonts:false}}).render(),outline=await Jimp.read(native.asPng()),painting=await Jimp.read(path.join(asset,'gnome-halloween-painted-generated.png'));painting.resize(w,h,Jimp.RESIZE_BICUBIC);await painting.writeAsync(path.join(asset,'painting-normalized.png'));await outline.writeAsync(path.join(asset,'outline-canonical-white.png'));
 const ids=new Int32Array(n),q=new Int32Array(n),regions=[];for(let i=0;i<n;i++)if(outline.bitmap.data[i*4]<165)ids[i]=-1;
 for(let i=0;i<n;i++){if(ids[i])continue;let id=regions.length+1,head=0,tail=1;q[0]=i;ids[i]=id;let count=0,sx=0,sy=0,x0=w,y0=h,x1=0,y1=0;while(head<tail){const p=q[head++],x=p%w,y=(p/w)|0;count++;sx+=x;sy+=y;x0=Math.min(x0,x);y0=Math.min(y0,y);x1=Math.max(x1,x);y1=Math.max(y1,y);for(const j of [x?p-1:-1,x<w-1?p+1:-1,y?p-w:-1,y<h-1?p+w:-1])if(j>=0&&!ids[j]){ids[j]=id;q[tail++]=j;}}regions.push({id,count,cx:sx/count,cy:sy/count,box:[x0,y0,x1,y1]});}
 const font=await Jimp.loadFont(Jimp.FONT_SANS_16_BLACK),label=outline.clone();for(const r of regions)if(r.count>60){const rr=(r.id*71)%100+155,gg=(r.id*39)%100+155,bb=(r.id*97)%100+155;for(let y=r.box[1];y<=r.box[3];y++)for(let x=r.box[0];x<=r.box[2];x++)if(ids[y*w+x]===r.id){const p=(y*w+x)*4;label.bitmap.data[p]=rr;label.bitmap.data[p+1]=gg;label.bitmap.data[p+2]=bb;}label.print(font,Math.round(r.cx)-8,Math.round(r.cy)-8,String(r.id));}
 await label.writeAsync(path.join(__dirname,'regions-review.png'));const bin=Buffer.alloc(8+n*4);bin.writeInt32LE(w);bin.writeInt32LE(h,4);for(let i=0;i<n;i++)bin.writeInt32LE(ids[i],8+i*4);fs.writeFileSync(path.join(__dirname,'regions.bin'),bin);fs.writeFileSync(path.join(__dirname,'regions.json'),JSON.stringify(regions,null,2));
 fs.writeFileSync(path.join(__dirname,'source-record.json'),JSON.stringify({source,sha256:crypto.createHash('sha256').update(fs.readFileSync(source)).digest('hex'),nativeDimensions:[2400,3000],maskDimensions:[w,h],physicalSizeInches:[8,10],trueVector:true,subpaths:(tag.match(/M/g)||[]).length},null,2));console.log(JSON.stringify({regions:regions.filter(r=>r.count>60),total:regions.length}));
})().catch(e=>{console.error(e);process.exit(1)});
