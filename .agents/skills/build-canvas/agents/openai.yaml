interface:
  display_name: "Build Canvas"
  short_description: "Build a complete paint kit from a subject folder"
  default_prompt: "Use $build-canvas to inspect the new subject folder in this project and build its complete canvas paint kit."
policy:
  allow_implicit_invocation: true
