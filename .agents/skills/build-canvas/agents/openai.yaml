interface:
  display_name: "Build Canvas"
  short_description: "Build an 8 x 10 paint kit and illustrated guide"
  default_prompt: "Use $build-canvas to inspect the subject folder and build its complete canvas paint kit, including 8 x 10 outline and finished-reference PDFs and PNGs plus a US Letter illustrated guide."
policy:
  allow_implicit_invocation: true
